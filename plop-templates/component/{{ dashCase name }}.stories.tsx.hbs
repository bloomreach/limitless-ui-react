import { Meta, StoryObj } from '@storybook/react';

import {{pascalCase name}} from './{{kebabCase name}}';

const meta: Meta<typeof {{pascalCase name}}> = {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
  tags: ['autodocs'],
  args: {
    /**
     * For the component properties which has `ReactNode` type set the default value as empty string
     * because the Storybook doesn't allow editing the `ReactNode` type properties in the controls.
     */
  },
};

export default meta;

export type Story = StoryObj<typeof {{pascalCase name}}>;

export const Basic: Story = {
  render: (args) => {
    return (
      <{{pascalCase name}}
        {...args}
      >
        {{pascalCase name}} component
      </{{pascalCase name}}>
    );
  },
  args: {
    className: 'custom-class-name',
  },
};
